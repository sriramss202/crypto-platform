import ReferralHero from "../Referral/ReferralHero";

function ReferralHeader({ activeTab, setActiveTab }) {
  return <ReferralHero activeTab={activeTab} setActiveTab={setActiveTab} />;
}

export default ReferralHeader;
